//- Text submission to query database for card name
//- Text submission to number for number in deck.
//- Text box with MtGA export style decklist, previous submissions add text to this box.
//- Ultimate submit button to save deck to database (users database?)

extends template

block content
    .deckBuilder
        form.cardInput(method="POST")
            input#title(type="text" placeholder="Deck Title")
            input.activePane#mainBoard(type="text")
            input.inactivePane#sideBoard(type="text")
            input#quantity(type="text" placeholder="Quantity")
            input#cardName(type="text" placeholder="Card Name")
            input#submit(type="submit") Submit
    
